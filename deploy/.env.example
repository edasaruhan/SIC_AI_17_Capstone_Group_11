# Gercek degerleri .env'e yaz; .env dosyasi Git'e EKLENMEZ.
# Anahtar uretmek icin: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEYS=change-me-key-1,change-me-key-2
RATE_LIMIT_PER_MIN=120
ALLOWED_ORIGINS=https://dashboard.example.com
PREDICTION_LOG=logs/predictions.jsonl
